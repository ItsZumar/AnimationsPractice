import {Dimensions} from 'react-native';

const {width} = Dimensions.get('window');

export const MENU_BUTTON_SIZE: number = 54;
export const CIRCLE_SCALE: number = 7;
// parseFloat(
//   (width / MENU_BUTTON_SIZE).toFixed(1),
// );
